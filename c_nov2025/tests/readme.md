# Unit tests

This directory contains unit test for the pipeline.

At the moment these are run locally using `pytest`.
But in a production environment this would be run during the CD pipeline before 
any deployment.

The test script only contains one test as an example.